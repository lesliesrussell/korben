[package]
name = "korben-syntax"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Reader, syntax objects, diagnostics, and canonical formatter for the Korben language"

[dependencies]
